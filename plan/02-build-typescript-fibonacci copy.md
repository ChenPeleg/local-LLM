## Build a TypeScript Fibonacci Function

### Objective
Create a simple, well-documented TypeScript function that calculates the Fibonacci sequence up to a given number.

### Steps
1. Define a function `fibonacci(n: number): number[]` that returns an array of Fibonacci numbers up to the nth number.
   - File location: `src/fibonacci.ts`
2. Add JSDoc comments to explain the function's purpose and parameters.
2. Add JSDoc comments to explain the function's purpose and parameters.
3. Include basic error handling for invalid inputs (e.g., negative numbers).
   - File location: `src/fibonacci.ts`
4. Test the function with a few examples to ensure correctness.

### Deliverables
- A working TypeScript file with the Fibonacci function.
   - File location: `src/fibonacci.ts`
- Unit tests (optional, but recommended).
- Documentation comments for the function.
   - File location: `src/fibonacci.ts`

### Notes
- Use TypeScript's type system to enforce input and output types.
- Keep the implementation simple and readable.
- Ensure the function handles edge cases gracefully.
- File location: `src/fibonacci.ts`
- File location: `src/fibonacci.ts`